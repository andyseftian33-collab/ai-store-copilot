{
  "name": "threads-1k-dashboard",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply threads-1k --local",
    "db:migrate:remote": "wrangler d1 migrations apply threads-1k --remote"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
